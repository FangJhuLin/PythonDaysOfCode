"""
thoughts:
    1. check if it's a number.
    2. check if it's zero.
    3. check if it's < 0.
    4. the rest are positive.
    5. Print the result.
"""


while True:
    #all the input are default as string type.
    x = input('Please give me a number, do not use"," please use"." as a decimal point:')
    
    try:
        #check if it's a number.
        x = float(x)
        
        if float(x) == 0:
            print('it\'s a zero.')
            
        elif float(x) < 0:
            print('it\'s negative.')
            
        else: 
            print('it\'s positive.')
            
    except ValueError:   
         print('it\'s not a number, try again.')  
